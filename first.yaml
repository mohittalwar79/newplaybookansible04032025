---
 - name: Newplaybook
   hosts: all
   become: true
   gather_facts: yes
   tasks:
     - name: copy file  
       copy:   
           src: /etc/rsyslog.conf
           dest: /tmp/
     - name : user create
       user:
           name: mohit199923
           home: /home/mohittalwar
           shell: /bin/bash
           comment: new user
     - name: ping check
       ping: 
            name: ping response
            debug: 
              msg: "error msg"
