---
 - name: Newplaybook
   hosts: all
   become: true
   gather_facts: yes
   tasks:
     - name: copy file  
       copy:   
           src: /etc/rsyslog.conf
           dest: /home/jenkins/
     - name : user create new
       user:
           name: mohit19992344
           home: /home/mohittalwar44
           shell: /bin/bash
           comment: new user
     - name: ping check
       ping: 
     - name: debug response
       debug: 
         msg: "error msg"
